{
  "version": "0.0.1",
  "name": "zap-rabbitmq",
  "description": "backend",
  "scripts": {
    "server": "nodemon server",
    "consumer": "nodemon consumer"
  },
  "dependencies": {
    "amqplib": "^0.8.0",
    "socket.io": "^4.4.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
